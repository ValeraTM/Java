package view;

import javax.swing.*;
import java.awt.*;
import java.awt.event.ActionEvent;

public class About extends JFrame {
    About() {
        /*this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        JPanel panel = new JPanel();
        panel.setLayout(new BorderLayout(50, 50));

        JPanel menu = new JPanel();
        panel.add(menu, BorderLayout.CENTER);

        JLabel info = new JLabel("Created by ");
        info.setFont(new Font("New NewGame", Font.PLAIN, 20));
        menu.add(info);

        this.repaint();
        this.getContentPane().add(panel);
        this.pack();
        //this.setSize(new Dimension(250, 270));
        //this.setVisible(true);


        JLabel info = new JLabel("<html><center>Created by<br />Malkhanov Valery</center></html>");
        info.setFont(mainFont);
        info.setHorizontalAlignment(SwingConstants.CENTER);
        aboutInfo.add(info, BorderLayout.CENTER);*/
    }
}
